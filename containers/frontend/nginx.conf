events{}

http {

    include /etc/nginx/mime.types;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        location /house-mix/ {
            try_files $uri $uri/ /house-mix/index.html;
        }
    }
}
