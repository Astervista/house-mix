// TODO: Preflight checklist
    Contains:
    build all docs,
    check docs for changes,
    check readmes for changes,
    check todos,
    tslint,
    versions,
    check for correctness in tsdoc
        regex to check for lowercase parameter def: @param (\{[^{}]*\}\s+)?[^ {}]*(\s+-)?\s+[a-z]
        regex to check for lowercase returns def: @returns?\s+(\{[^{}]*\}\s+)?\s*[a-z]
// TODO: Add to the readmes information on how to setup zigbee
// TODO: Search in Api docs
// TODO: Style examples
// TODO: Hide inherit-doc text
// TODO: Bundle needs to come with the proxy and maybe zigbee2mqtt, change readmes accordingly (maybe multiple options)
// TODO: Create zips for fast download from website
