import {ScrollOnSelectedDirective} from './scroll-on-selected.directive';

describe('ScrollOnSelectedDirective', () => {
    it('should create an instance', () => {
        const directive = new ScrollOnSelectedDirective();
        expect(directive).toBeTruthy();
    });
});
