import {InputReturnBehaviorDirective} from './input-return-behavior.directive';

describe('InputReturnBehaviorDirective', () => {
    it('should create an instance', () => {
        const directive = new InputReturnBehaviorDirective();
        expect(directive).toBeTruthy();
    });
});
