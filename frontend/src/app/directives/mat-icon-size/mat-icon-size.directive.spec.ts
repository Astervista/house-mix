import {MatIconSizeDirective} from "./mat-icon-size.directive.js";

describe("MatIconSizeDirective", () => {
    it("should create an instance", () => {
        const directive = new MatIconSizeDirective();
        expect(directive).toBeTruthy();
    });
});
